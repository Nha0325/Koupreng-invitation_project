# Gifts API

Verified controller: `WeddingGiftController`.

Base path: `/api/v1/invitations/{invitationId}/gifts`

Routes:
- `GET /` role: `USER`
- `POST /` role: `USER`
- `PUT /{giftId}` role: `USER`
- `DELETE /{giftId}` role: `USER`

Ownership:
- Wedding gifts are scoped to one `invitationId`.

Schemas:
- Insufficient data to verify full request and response schemas in this document.
