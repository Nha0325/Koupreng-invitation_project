global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: koupreng-backend
    scheme: https
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - api.example.com
