# Legacy Admin Services

The active admin service is `src/features/admin/adminManagementService.js`
plus feature-local services such as `src/features/notifications/notificationService.js`.

Files in this folder are older compatibility services kept temporarily for
reference. Do not use them for new active admin screens unless the endpoint is
confirmed to remain supported.
