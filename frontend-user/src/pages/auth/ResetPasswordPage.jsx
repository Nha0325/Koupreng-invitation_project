// TODO(task 13.3): Wire up password reset flow via authService.resetPassword.
// This is a placeholder created in task 6.2 so the router can lazy-load
// the route at /reset-password without crashing.

const ResetPasswordPage = () => {
    return (
        <main className="reset-password-page">
            <h1>Reset password</h1>
        </main>
    );
};

export default ResetPasswordPage;
