// Vitest global setup. Extends jsdom's `expect` with @testing-library/jest-dom matchers
// (e.g., `toBeInTheDocument`, `toHaveClass`) so they're available in every test file.
import '@testing-library/jest-dom'
