# Khmer locale profile.
# Activate with SPRING_PROFILES_ACTIVE=km or --spring.profiles.active=km.

app.i18n.default-locale=km
app.i18n.supported-locales=km,en
spring.messages.basename=messages
spring.messages.encoding=UTF-8
spring.messages.fallback-to-system-locale=false
